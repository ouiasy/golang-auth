services:
  postgres-test:
    image: postgres:14.19-alpine3.21
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: test_db
    ports:
      - "5432:5432"
    volumes:
      - test-db-data:/var/lib/postgresql/data
    
volumes:
  test-db-data: